def bubble_sort(array)
  swap = 0 
  
  array.each_index do |x|
    do (array.length -1).times 
  if array[x + 1] < array[x]
    array[x], array[x + 1] = array[x + 1], array[x]
  end
end
end
#array
end

bubble_sort([30, 15, 6, 8, 10, 3])
  
